def f(x: int) -> int:
    if x:= x > 0:
        return x  # E: Incompatible return value type (got "bool", expected "int")
    return 0
print(f(7))

# flags: --strict-bool
def g(a: int):
    return
print(g(True))  # E: Argument 1 to "f" has incompatible type "bool"; expected "int"

# flags: --strict-bool
def h() -> int:
    return (True) # E: Incompatible return value type (got "bool", expected "int")
print(h())
